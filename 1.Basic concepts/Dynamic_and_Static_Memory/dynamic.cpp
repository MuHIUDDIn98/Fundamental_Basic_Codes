#include<bits/stdc++.h>

using namespace std;

int main()

{

 int *x = new int; //creating a dynamic memory in heap and declear a pointer x , keep the address of that dynamic memory in pointer p

 *x = 10; // dereferencing the pointer we keep the value 10 in that pointer.

 cout << *x << endl; 
}
